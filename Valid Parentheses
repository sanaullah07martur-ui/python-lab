s = "({[]})"
stack = []
mapping = {')':'(', '}':'{', ']':'['}

valid = True
for c in s:
    if c in mapping:
        if not stack or stack.pop() != mapping[c]:
            valid = False
            break
    else:
        stack.append(c)

print(valid and not stack)